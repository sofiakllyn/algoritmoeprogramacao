void main() {
    Scanner input = new Scanner(System.in);
    
    double x = 0;
    double X2 = x * x;
    double X3 = x * x * x;

    System.out.print("Digite o valor de X: ");
    x = input.nextDouble();

    if (x < 1) {
        System.out.println( "F(X)" + 1);
    } else if ( x >= 1 && x <= 2) {
        System.out.println("F(X)" +2);
    } else if (x >= 2 && x <= 3) {
        System.out.println("F(X)"  + (x*x));
    } else {
        System.out.println("F(X)" + (x*x*x));
    }
}
